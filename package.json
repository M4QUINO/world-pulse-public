{
  "name": "world-pulse",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:render": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start:render": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=20"
  }
}
